services:
  site:
    build: .
    ports:
      - "3000:3000"
    environment:
      HOST: 0.0.0.0
      GITHUB_TOKEN: ${GITHUB_TOKEN:-}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
