---
title: Parity matrix
description: Capability status and Linux replacement decisions.
---


The application repository's `PARITY.md` is the maintenance source for capability status, acceptance checks, and Linux replacement decisions. This page is the single rendered status authority for the documentation site.

## What the badges mean

Every workflow in `PARITY.md` is marked `done`, or carries a decision status for workflows without a Linux equivalent:

- <StatusBadge status="done" /> means the workflow is usable end to end on Linux.
- Decision rows ("Not applicable", "Replaced by ...", "Use ... instead") mark Windows-only workflows with no Linux equivalent; they are documented as boundaries rather than implied support.

## Capability status

The source matrix lists 111 LaunchBox-aligned capabilities as <StatusBadge status="done" /> (including Localization with 5 languages: English, Spanish, German, French, and Portuguese), including the Premium-equivalent workflows (custom fields, ESRB filters, list view, media packs, cloud statistics sync), all without a subscription. OpenBoxGL sets `premium_features_free: true` in settings and ships bundled media packs without a license gate.

| Area | Status | Notes |
| --- | --- | --- |
| Mixed-library management | <StatusBadge status="done" /> | One catalog for Steam, ROMs, arcades, executables |
| Imports | <StatusBadge status="done" /> | Steam, Heroic, Lutris, Faugus, Gameyfin, ROM folders, arcade DATs, ScummVM, RPCS3, Vita3K |
| Metadata | <StatusBadge status="done" /> | LaunchBox Games Database daily sync + IGDB + Steam/GOG |
| Media | <StatusBadge status="done" /> | Bulk media jobs, duplicate cleanup, EmuMovies, Bezel Project |
| Launch profiles | <StatusBadge status="done" /> | Tokenized commands, per-game overrides, archive extraction, dependency checks, Eden Switch |
| Big Box | <StatusBadge status="done" /> | Stage, Hybrid, CoverFlow; gamepad; screensaver; gamescope guest |
| Sessions and tracking | <StatusBadge status="done" /> | Per-session history, tracking modes, progress automation |
| Saves and backups | <StatusBadge status="done" /> | Save discovery, versioned backups, restore guards, statistics sync |
| Integrations | <StatusBadge status="done" /> | Proton/Wine Prefix Manager, RetroAchievements, IGDB, EmuMovies, Bezel, Gameyfin, OBS, MAME, Ludusavi, Hoard |
| Plugins | <StatusBadge status="done" /> | `library`, `before_launch`, `after_session` hooks |
| Local automation | <StatusBadge status="done" /> | Queue, tags, notifications, HMAC-signed webhooks |
| Premium workflows (free) | <StatusBadge status="done" /> | Custom fields, ESRB filters, list view, media packs, cloud stats sync |
| Discovery (v1.9.0) | <StatusBadge status="done" /> | "What should I play?" picker, Constellation graph, SQLite FTS search |
| Insights (v1.9.0) | <StatusBadge status="done" /> | Wrapped year-in-review, Mastery Map, history timeline |
| Party & personalization (v1.9.0) | <StatusBadge status="done" /> | Game Night party mode, Big Box video snaps, Mood Match adaptive theming |
| Migration & sync (v1.9.0) | <StatusBadge status="done" /> | LaunchBox XML import, full-library mounted-folder sync, manual shelf entries |

## Intentionally not replicated on Linux

Windows-only workflows without a Linux equivalent remain documented as boundaries rather than implied support:

| LaunchBox feature | OpenBox decision |
| --- | --- |
| Windows shell replacement | Not applicable on Linux desktop environments |
| LEDBlinky / cabinet LED control | Use external Linux arcade I/O tools instead |
| Teknoparrot arcade launcher | Use Lutris/Wine launch profiles for supported titles |
| Native Xbox PC package scanning | Use Heroic/Lutris/Xbox Cloud entries instead |
| Bundled proprietary media packs | Replaced by free bundled media packs in OpenBox |
| LaunchBox Premium account cloud library | Replaced by OpenBox's own mounted-folder sync plus local backups (no LaunchBox account involved) |
| LaunchBox online theme storefront | Replaced by local CSS theme import and open-folder workflow |

## How to read the evidence

Each `done` row carries an acceptance check, and the parity test modules (`test_parity_*.py`) exercise the underlying helpers. The authoritative row-level evidence lives in the source parity matrix:

[Source parity matrix](https://github.com/vindeckyy/OpenBoxGL/blob/master/docs/PARITY.md)

## Status updates

When behavior changes, `PARITY.md` is updated in the application repository and this page follows it. Do not claim a capability here before the source matrix and its acceptance check agree.

## Parity internals

The capability rows above are implemented by 27 modules in `pkg/parity/` (application repository), each with a root compatibility shim. This is an inventory, not a second status matrix — status still lives in the table above.

| Module | Area |
| --- | --- |
| `parity_backup.py` | Library backups |
| `parity_deeplinks.py` | Deep links |
| `parity_discovery.py` | Discovery queue |
| `parity_emulator_defs.py` | Emulator-def registry |
| `parity_export.py` | Library export |
| `parity_faugus.py` | Faugus integration |
| `parity_filter_presets.py` | Filter presets |
| `parity_gamescope.py` | Gamescope guest |
| `parity_gameyfin.py` | Gameyfin storefront |
| `parity_identity.py` | Identity / instance |
| `parity_igdb.py` | IGDB metadata |
| `parity_import.py` | Imports |
| `parity_import_policy.py` | Import policy |
| `parity_insights.py` | Play insights |
| `parity_integrations.py` | Third-party integrations |
| `parity_launch_doctor.py` | Launch Doctor preflight |
| `parity_media.py` | Media jobs |
| `parity_perf.py` | Performance projections |
| `parity_premium.py` | Premium-equivalent workflows |
| `parity_redact.py` | Log redaction |
| `parity_saves.py` | Saves |
| `parity_save_tools.py` | Save tools (Ludusavi/Hoard) |
| `parity_screenscraper.py` | ScreenScraper matching |
| `parity_setup_preview.py` | Setup Center preview |
| `parity_storefront.py` | Storefront imports |
| `parity_tracking.py` | Play tracking |
| `parity_wine.py` | Wine/Proton prefix management |
